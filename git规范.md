# Git Commit Message 提交注释规范
## 一、完整提交格式
```
<type>(<scope>): <subject>

<body>
```
字段说明：
1. **type**：提交类型（必填）
2. **scope**：影响模块/范围（选填，小型项目可省略）
3. **subject**：简短单行描述，50字符以内（必填）
4. **body**：改动详细说明，每行72字符换行（选填，复杂改动填写）

## 二、type 提交类型枚举
| 类型 | 含义 | 使用场景 |
|------|------|--------|
| feat | 新功能 | 新增页面、接口、组件、业务能力 |
| fix | Bug修复 | 线上/测试bug、逻辑错误、数据异常 |
| docs | 文档修改 | 注释、README、接口文档、说明文案 |
| style | 格式调整 | 空格、换行、分号、代码格式化，无业务逻辑改动 |
| refactor | 代码重构 | 逻辑不变，优化结构、抽离工具类、重命名变量方法 |
| perf | 性能优化 | 查询耗时、加载速度、内存占用、渲染效率优化 |
| test | 单元/集成测试 | 新增测试用例、修复测试代码、提升测试覆盖率 |
| chore | 工程维护 | 构建脚本、依赖升级、配置文件、CI/CD、Git配置 |
| revert | 回滚提交 | 撤销某次历史commit |

## 三、scope 范围自定义规则
按项目业务分层填写，示例参考：
- 后端：`user`、`order`、`pay`、`dts`、`gateway`、`db`
- 前端：`login`、`dashboard`、`table`、`api`、`components`
- 公共模块：`common`、`utils`、`config`
  无明确归属模块可省略scope：`feat: 新增全局弹窗组件`

## 四、subject 标题强制规范
1. 文字长度控制在 **50 字符内**
2. 动词原形开头：add / modify / fix / optimize / remove
3. 句尾不添加句号、逗号
4. 中文项目可使用中文描述，动词前置

✅ 正确示例：`feat(user): 新增手机号登录接口`
❌ 错误示例：`feat：新增用户登录功能。`（符号错误、带句号）
❌ 错误示例：`feat(user): 用户登录新增接口，用于账号登录验证`（描述冗长）

## 五、body 正文书写规范（复杂改动必填）
1. 与首行标题空一行分隔
2. 单行文字不超过72字符，超长自动换行
3. 说明改动背景、原有问题、实现方案
4. 使用 `-` 符号分点罗列改动内容

示例：
```
fix(order): 修复订单支付状态同步延迟问题

- 原MQ消费无重试机制，消息丢失导致订单状态未更新
- 新增本地事务补偿表，定时同步未完成支付订单
- 配置消息投递失败自动重试3次，间隔5分钟
```

## 六、标准提交示例
### 1. 简单单行提交（微小改动，无需body）
```
feat(login): 新增短信验证码登录功能
```
```
fix(api): 修复host配置读取异常问题
```
```
chore: 统一升级项目Maven依赖版本
```

### 2. 完整多行提交（新增功能、重大bug修复）
```
feat(order): 实现订单批量导出Excel能力

- 新建导出任务数据表，采用异步任务生成文件
- 限制单次导出最大1000条订单，避免服务OOM
- 提供导出记录查询、文件下载、任务删除接口
```

### 3. 代码回滚提交
```
revert: feat(pay): 集成微信支付渠道

This reverts commit a2d567f89b342e15645c21f9d48c351
```

## 七、团队使用约束
1. 禁止模糊无意义备注：`update`、`改代码`、`调试`、`临时提交`
2. 单次提交只做一类改动，新增功能与bug修复拆分不同commit
3. 固定格式：`type(scope): 描述`，冒号后方必须加空格
4. 线上紧急修复bug统一使用hotfix作为scope：`fix(hotfix): xxx`
5. 可搭配 husky + commitlint 工具强制校验提交格式，不合规禁止提交

## 八、配套工具推荐
1. commitlint + husky：提交时自动校验注释格式
2. cz-git / commitizen：交互式命令行快速生成规范commit
3. VSCode插件 Git Commit Message Helper：可视化选择type快速编写备注